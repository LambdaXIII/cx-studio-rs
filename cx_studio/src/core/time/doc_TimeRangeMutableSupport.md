Representing a mutable time range object.

TimeRangeMutableSupport depends on the [TimeRangeSupport] trait.
It provides functions to modify the start time and the duration.

It also provides a function to set the times from a [TimeRange] object.
